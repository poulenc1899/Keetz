import { createTheme } from '@mui/material/styles';

   const theme = createTheme({
     palette: {
       primary: {
         main: 'rgb(158, 89, 218)',
       },
       // You can also customize other palette properties
     },
     // You can also customize other theme aspects like typography, spacing, etc.
   });

   export default theme;